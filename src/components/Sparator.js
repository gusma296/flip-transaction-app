import React from 'react';
import {Colors} from '../common';
import Content from './Content';

const Sparator = () => {
  return <Content height={1} width={'100%'} backgroundColor={Colors.divider} />;
};

export default Sparator;
